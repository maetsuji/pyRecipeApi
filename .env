DATABASE_URL=mysql+pymysql://root:root@db:3306/recipes

### Explanation:
# 1. **Username**: The username is `root`, as specified in the `db` service environment variables in `docker-compose.yaml`.
# 2. **Password**: The password is `root`, as defined by `MYSQL_ROOT_PASSWORD` in the `db` service.
# 3. **Host**: The hostname is `db`, which matches the service name in `docker-compose.yaml`.
# 4. **Port**: The port is `3306`, the default MySQL port.
# 5. **Database Name**: The database name is `recipes`, as defined by `MYSQL_DATABASE` in the `db` service.